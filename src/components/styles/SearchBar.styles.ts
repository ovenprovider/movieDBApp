import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    padding: 8,
    border: 'black',
    borderWidth: 1,
    borderRadius: 6,
    backgroundColor: 'white'
  }
})

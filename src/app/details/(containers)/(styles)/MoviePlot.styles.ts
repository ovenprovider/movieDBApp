import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  moviePlotLabelText: {
    fonSsize: 18,
    marginTop: 8
  },
  moviePlotTextContainer: {
    marginTop: 4
  }
})

import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  movieYearContainer: {
    marginTop: 8
  },
  movieYearText: {
    fontSize: 16,
    textAlign: 'left',
    color: 'gray'
  }
})
